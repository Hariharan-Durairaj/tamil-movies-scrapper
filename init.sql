-- This file runs once on first startup (only when the data volume is empty).
-- The application's Database.init_db() method creates all tables automatically,
-- so nothing extra is needed here.
-- Add any one-time seed data below if required.

-- Example: create a read-only reporting user
-- CREATE ROLE reporter WITH LOGIN PASSWORD 'reporter_pass';
-- GRANT CONNECT ON DATABASE movie_automator TO reporter;
